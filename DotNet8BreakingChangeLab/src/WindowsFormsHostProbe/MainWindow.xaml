<Window x:Class="WindowsFormsHostProbe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        mc:Ignorable="d"
        Title="WindowsFormsHost Probe (.NET 8)" Height="520" Width="980">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="14" TextWrapping="Wrap">
            This app hosts a WinForms control inside WPF via WindowsFormsHost.
            Some users report rendering issues in .NET 8 when combined with certain Fluent themes or styling.
            Use this project as a minimal reproduction harness.
        </TextBlock>

        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Host" Margin="0,0,8,0">
                <StackPanel Margin="10" >
                    <TextBlock TextWrapping="Wrap" Margin="0,0,0,10">
                        If you use a Fluent theme library (e.g., WPF-UI), add it to this project and compare .NET 6 vs .NET 8.
                        Look for: black rectangles, wrong background, flicker, z-order issues.
                    </TextBlock>

                    <Border BorderBrush="Gray" BorderThickness="1" Padding="8" Margin="0,0,0,10">
                        <wfi:WindowsFormsHost x:Name="Host" Height="120"/>
                    </Border>

                    <Button Content="WPF Button (for contrast)" Width="220" Height="32"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Log" Grid.Column="1">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6">
                        <Button Content="Clear" Width="90" Margin="0,0,6,0" Click="Clear_Click"/>
                        <Button Content="Toggle WinForms BackColor" Width="190" Margin="0,0,6,0" Click="ToggleColor_Click"/>
                    </StackPanel>
                    <TextBox x:Name="LogTextBox" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                </DockPanel>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
